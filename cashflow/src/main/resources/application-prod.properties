# Let Render control the port (critical)
server.port=${PORT:8080}

# ←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←
# DELETE ALL THESE LINES – we now use DATABASE_URL only
# spring.datasource.url=...
# spring.datasource.username=...
# spring.datasource.password=...
# spring.datasource.driver-class-name=...
# spring.jpa.properties.hibernate.dialect=...
# ←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←

# These three are completely fine and can stay
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# OAuth2 – perfect as-is
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,email,profile
spring.security.oauth2.client.registration.google.redirect-uri=https://cashflow-4xky.onrender.com/login/oauth2/code/google

# Your frontend redirect after login
app.oauth2.redirect-url=https://cashflow-4xky.onrender.com/dashboard.html

# JWT – also perfect
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}
